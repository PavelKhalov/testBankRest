spring:
  application:
    name: testBankRest
  datasource:
    password: postgres
    url:
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop


jwt:
  secretKey: ${JWT_SECRET}